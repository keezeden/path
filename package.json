{
  "private": true,
  "name": "path-finding",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "build": "parcel build --no-cache",
    "start": "parcel ./src/index.html --no-cache"
  },
  "dependencies": {
    "preact": "^10.1.0",
    "tailwindcss": "^1.5.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.4"
  }
}
